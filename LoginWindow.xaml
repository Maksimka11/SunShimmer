<Window x:Class="SunShimmer.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunShimmer"
        mc:Ignorable="d"
        Title="Авторизация" Height="350" Width="600" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Style="{StaticResource BaseWindow}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Label Content="Email" Grid.Row="0" HorizontalContentAlignment="Center"/>
        <TextBox x:Name="TbEmail" Grid.Row="0" Grid.Column="1"/>

        <Label Content="Пароль" Grid.Row="1" HorizontalContentAlignment="Center"/>
        <PasswordBox x:Name="PbPassword" Grid.Row="1" Grid.Column="1"/>

        <Button x:Name="BtnLogin" Content="Войти" Click="BtnLogin_Click" Grid.Row="2"/>
        <Button x:Name="BtnRegistration" Content="Создать аккаунт" Click="BtnRegistration_Click" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</Window>
